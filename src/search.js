import React from 'react';

const search = ({ counter }) => {
    return (
        <div>
            <input type="text" />
            <h1>Hey {counter}</h1>
        </div>
    );
};

export default search;